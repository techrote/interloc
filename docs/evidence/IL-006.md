# IL-006 evidence — enrolled command and log collection

## Objective
Implement issue #7's explicitly enrolled local collection layer without global terminal/process surveillance.

## Inputs/base SHA
Prerequisite/base: `6962d752098484f2777d6d25e386998fa87afcc4` with IL-002 merged and evidenced.
Implementation SHA: `accc5be664505be0de5e1f8a8eedb1a6906a51fb` / PR #37.
Merged main SHA: `26fb1577a31b9c173047b9e1e2737ef9b6c66230`.

## Implemented scope
Supervised argv-based child collection with bounded queues/chunks and incremental decoding; explicit source/session/repository/worktree aliases; before/after read-only Git snapshots; timeout/cancellation/reaping; allowlisted child environment; exact enrolled log file tails; partial multibyte decoding; rotation/truncation/gap handling; bounded cursor continuation.

## Non-goals
No arbitrary existing-terminal attachment, shell-string execution, OCR/keylogging, global process enumeration, network publication, remote execution authority or redaction claims.

## Changed files
`src/interloc/collectors/`, `tests/collectors/`.

## Environment
Local: Linux, CPython 3.13.5. Collector suite was rerun with `ResourceWarning` promoted to error. GitHub Actions run `34729019660`: Ubuntu/Windows on Python 3.12/3.13/3.14.

## Commands/results
- `PYTHONWARNINGS=error::ResourceWarning PYTHONPATH=src python -m unittest discover -s tests/collectors -v` — PASS, 12/12 collector tests.
- Actions run `34729019660` — PASS across all six OS/Python jobs, including clean install, full unittest suite, CLI smoke and workflow audit.

T-collect coverage includes stdout/stderr Unicode without newline, malformed-byte replacement, credential non-inheritance, timeout and cancellation cleanup, 200k output flood with bounded event chunks, independent session/worktree attribution, explicit before/after Git-state change, partial UTF-8 file tails, truncation/rotation, path escape denial, bounded log cursor continuation and a real temporary Git worktree smoke where Git is available.

## Native/external checks
Windows execution passed in GitHub-hosted Windows jobs. The tests exercise subprocess pipes, Unicode/spaced temp paths and Git discovery cross-platform. No user terminal buffer was attached or scraped.

## Security review
Children receive only a small environment allowlist; GH/GITHUB tokens, SSH agent variables and API-token names are not forwarded. Commands use argv with `shell=False`. Log sources are exact locally enrolled absolute paths and must resolve under the enrolled cwd. Git inspection disables optional locks and terminal prompting. Absolute local paths remain internal collector data for later sanitization.

## Gate decisions
IL-006 acceptance criteria and T-collect requirements are satisfied by implementation SHA `accc5be664505be0de5e1f8a8eedb1a6906a51fb` and Actions run `34729019660`. The collector can feed IL-007/IL-010/IL-018 only through their own later evidence/policy boundaries.

## Remaining blockers
None for IL-006.

## Reproduction
From a clean merged checkout run `python -m unittest discover -s tests -v`, then the workflow audit separately. For leak/resource warnings on a development machine, rerun `PYTHONWARNINGS=error::ResourceWarning python -m unittest discover -s tests/collectors -v` where supported. Authoritative cross-platform evidence is Actions `34729019660`.
