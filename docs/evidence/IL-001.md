# IL-001 evidence — development foundation

## Objective
Establish the minimal reproducible Python/Windows/Linux development foundation required by issue #2.

## Inputs/base SHA
Base `main`: `8d1d86f04bf76ce803c092f295a33ea7a3378611`.
Implementation tested SHA: `8a9311cff4b8f122ac5f805d9c1aada99e46e61d` on branch `codex/il-001-foundation` / PR #32.

## Implemented scope
Installable Python 3.12+ package; `interloc` entry point; read-only `doctor`; bounded local configuration primitives; optional native-provider discovery without eager import; standard-library unit tests; pinned build dependencies; Windows/Linux CI; development instructions.

## Non-goals
No mailbox transport, request execution, screenshots, GitHub credentials, browser integration, local service, or later-issue capability behavior.

## Changed files
`pyproject.toml`, `requirements-dev.lock`, `src/interloc/*`, `tests/test_foundation.py`, `.github/workflows/ci.yml`, `docs/DEVELOPMENT.md`.

## Environment
Local pre-publication checks: Linux, CPython 3.13.5. The local tool sandbox has no outbound package-index access, so isolated installation there was attempted and correctly recorded as unavailable rather than passed.

GitHub Actions run `34728271840` tested Ubuntu and Windows on Python 3.12, 3.13 and 3.14.

## Commands/results
- `PYTHONPATH=src python -m unittest discover -s tests -v` — PASS, 7/7 tests.
- Local isolated `pip install .` — NOT RUN to completion: build-isolation dependency download was blocked by the sandbox's disabled outbound network.
- GitHub Actions `python -m pip install .` — PASS across the six OS/Python matrix jobs.
- GitHub Actions `python -m unittest discover -s tests -v` — PASS across the matrix.
- GitHub Actions `interloc --help` and `interloc doctor --json` — PASS across the matrix.
- GitHub Actions `python tools/audit_workflow.py` — PASS across the matrix.

## Native/external checks
Windows installation/CLI checks: PASS through GitHub-hosted `windows-latest` on Python 3.12/3.13/3.14. No user-machine Windows test was performed or claimed.
No credentials or external services were required.

## Security review
`doctor` is read-only and does not create state, authenticate, install dependencies, or eagerly import absent native providers. Runtime-state names remain ignored from source control. CI permissions are `contents: read` only. No runtime/test third-party dependency was introduced.

## Gate decisions
IL-001 acceptance criteria are satisfied at tested implementation SHA. Later issues may use this foundation only after PR #32 is merged.

## Remaining blockers
None for IL-001. Native feature providers are intentionally absent and belong to later issues.

## Reproduction
Follow `docs/DEVELOPMENT.md`. Run each command separately as documented. The authoritative clean-install evidence for this issue is Actions run `34728271840` against implementation SHA `8a9311cff4b8f122ac5f805d9c1aada99e46e61d`.
