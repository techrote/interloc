# IL-003 evidence — local enrollment and policy

## Objective
Implement issue #4's deny-by-default local authority model so remote request attribution cannot confer execution/publication authority.

## Inputs/base SHA
Prerequisite/base: `6962d752098484f2777d6d25e386998fa87afcc4` with IL-002 merged and evidenced.
Implementation SHA: `4e1a9e046798cceea57750ee490031cf3a196d5b` / PR #36.
Merged main SHA: `9ceb38b44c55dbff249804664cd417d1969760e6` (subsequently included in current main).

## Implemented scope
Pure local enrollment/policy dataclasses; exact capability/scope/destination grants; private numeric mailbox identity, epoch/device/branch matching; allow/confirm/deny decisions; request/policy/target-state/expiry-bound approvals; revocation; and locally scoped path resolution with traversal/drive/UNC/ADS/symlink-escape rejection.

## Non-goals
No network, broker dispatch, user interface, screenshots, clipboard, arbitrary shell, remote policy edits or real target actions.

## Changed files
`src/interloc/policy/`, `tests/policy/`.

## Environment
Local: Linux, CPython 3.13.5. GitHub Actions run `34728857329`: Ubuntu/Windows on Python 3.12/3.13/3.14.

## Commands/results
- `PYTHONPATH=src python -m unittest discover -s tests -v` — PASS, 30/30 tests at implementation time (10 policy + 20 prior tests).
- Actions run `34728857329` — PASS across all six OS/Python jobs, including clean install, full tests, CLI smoke and workflow audit.

T-policy coverage includes forged requester labels, repository/branch/epoch/device/destination mismatch, missing/deny grants, forbidden wildcard/unknown grants, public-mailbox rejection, visual capture confirmation requirement, exact request digest binding, policy-revision/target-state/expiry/revocation invalidation, exact terminal session scope, traversal/absolute/UNC/ADS paths and current symlink escape.

## Native/external checks
Windows package/test execution passed in GitHub-hosted Windows jobs. No user desktop/native capability was required or claimed.

## Security review
`requester_label` is ignored for authority. Transport origin plus local enrollment are required. `capture.window` cannot be configured as globally auto-allow. Grants reject wildcard scope. Only the enrolled mailbox destination is supported. Local path resolution documents that later file users must revalidate at use time for race resistance.

## Gate decisions
IL-003 acceptance criteria and T-policy are satisfied by implementation SHA `4e1a9e046798cceea57750ee490031cf3a196d5b` and successful Actions run `34728857329`. IL-004, IL-005, IL-007 and IL-012 may consume the merged policy interface when their other prerequisites are met.

## Remaining blockers
None for IL-003.

## Reproduction
Install a clean checkout and run `python -m unittest discover -s tests -v`, then `python tools/audit_workflow.py` separately. The authoritative cross-platform run is Actions `34728857329`.
